{
  "name": "solver-js-demo",
  "version": "0.1.0",
  "type": "module",
  "description": "Node.js demo for using the solver WASM module",
  "scripts": {
    "build": "tsc && cp solver/_solver.wasm dist/solver/_solver.wasm",
    "start": "npm run build && node dist/test.js"
  },
  "devDependencies": {
      "ts-node": "^10.9.2",
      "typescript": "^5.7.3"
  }
}
